{
  "name": "resumecanvas",
  "version": "1.0.0",
  "private": true,
  "description": "ResumeCanvas — a free resume builder for job seekers.",
  "scripts": {
    "test": "node tests/smoke.js",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0"
  }
}
